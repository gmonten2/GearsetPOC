<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>bt_stripe__Live_Validation_Mode__c</fullName>
    <defaultValue>false</defaultValue>
    <deprecated>false</deprecated>
    <description>If checked, Payment Method will use &quot;liveMode&quot; to submit a zero-dollar or one-cent transaction (depending on card type and processor support) to confirm the card number belongs to an active credit or debit account. If unchecked, it will use &quot;testMode&quot; to perform a Luhn mod-10 check on the card number, without further validation.</description>
    <inlineHelpText>If checked, Payment Method will use &quot;liveMode&quot; to submit a zero-dollar or one-cent transaction (depending on card type and processor support) to confirm the card number belongs to an active credit or debit account. If unchecked, it will use &quot;testMode&quot; to perform a Luhn mod-10 check on the card number, without further validation.</inlineHelpText>
    <label>Live Validation Mode (Authorize.Net)</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>

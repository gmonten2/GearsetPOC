<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>bt_stripe__Start_Date__c</fullName>
    <deprecated>false</deprecated>
    <description>The first date from which the remainder of the logic is calculated. It can be the date of the first Transaction or the first date to calculate subsequent Transactions from.</description>
    <inlineHelpText>The first date from which the remainder of the logic is calculated. It can be the date of the first Transaction or the first date to calculate subsequent Transactions from.</inlineHelpText>
    <label>Start Date</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>

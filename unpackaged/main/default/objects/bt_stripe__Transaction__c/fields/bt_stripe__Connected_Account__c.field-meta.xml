<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>bt_stripe__Connected_Account__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <deprecated>false</deprecated>
    <description>May serve as the source if for a Payout, the destination if for a Transfer, or the source if for a Transfer Reversal.</description>
    <inlineHelpText>May serve as the source if for a Payout, the destination if for a Transfer, or the source if for a Transfer Reversal.</inlineHelpText>
    <label>Connected Account</label>
    <referenceTo>bt_stripe__Connected_Account__c</referenceTo>
    <relationshipLabel>Transactions</relationshipLabel>
    <relationshipName>Transactions</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
